{
  "name": "{{REPO_NAME}}",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "4"
  }
}
